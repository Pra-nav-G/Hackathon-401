# FastAPI web framework
fastapi==0.104.1

# ASGI server for FastAPI
uvicorn[standard]==0.24.0

# Async MongoDB driver
motor==3.3.2

# Object Document Mapping and data validation
pydantic[email]==2.5.0

# Environment variable management
python-dotenv==1.0.0

# MongoDB ObjectId handling (included with pymongo, motor dependency)
pymongo==4.6.0

# CORS middleware (included with FastAPI)
# fastapi already includes starlette which provides CORSMiddleware

# Development dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2  # for testing FastAPI endpoints